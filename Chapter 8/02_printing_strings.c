#include<stdio.h>

int main(){
    // char st[] = {'a', 'b', 'c'};
    char st[] = {"abc"};    // Same as char st[] = {'a', 'b', 'c'};
    // for (int i = 0; i < 3; i++)
    // {
    //     printf("%c", st[i]);
    // }
    
    printf("%s", st);

    return 0;
}